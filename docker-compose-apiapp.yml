version: '3'
services:
  apicallapp:
    image:
    networks:
      - mynetwork
    ports:
      - "80:8000"
    environment:
      - CALL_ME_HOST=http://callme:8001/callme
      - HOST_IP=apicallapp
    restart: unless-stopped

networks:
  mynetwork:

