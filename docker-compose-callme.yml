version: '3'
services:
  callme:
    image:
    networks:
      - mynetwork 
    ports:
      - "80:8000"
    environment:
      - DJANGO_ALLOWED_HOSTS=
    restart: unless-stopped
    hostname: callme

networks:
  mynetwork: